class KNN(object):
	"""docstring for ClassName"""
	def __init__(self,k):
		self.k = k
	def fit(self,dadosX,dadosY):
		self.dadosX = dadosX
		self.dadosY = dadosY
	def predict(self,dadosX):
		for i in self.dadosX:
			for j in self.dados 
		
		